<%= form_for(unidad) do |f| %>
  <% if unidad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(unidad.errors.count, "error") %> prohibited this unidad from being saved:</h2>

      <ul>
      <% unidad.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre_unidad %>
    <%= f.text_field :nombre_unidad %>
  </div>

  <div class="field">
    <%= f.label :competencias %>
    <%= f.text_area :competencias %>
  </div>

  <div class="field">
    <%= f.label :horas_unidad %>
    <%= f.number_field :horas_unidad %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
