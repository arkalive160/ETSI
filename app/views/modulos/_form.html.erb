<%= form_for(modulo) do |f| %>
  <% if modulo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(modulo.errors.count, "error") %> prohibited this modulo from being saved:</h2>

      <ul>
      <% modulo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="field">
    <%= f.label :horas_modulo %>
    <%= f.number_field :horas_modulo %>
  </div>

  <div class="field">
    <%= f.label :descripcion %>
    <%= f.text_area :descripcion %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
