<%= form_for(procedimiento) do |f| %>
  <% if procedimiento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(procedimiento.errors.count, "error") %> prohibited this procedimiento from being saved:</h2>

      <ul>
      <% procedimiento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titulo %>
    <%= f.text_field :titulo %>
  </div>

  <div class="field">
    <%= f.label :descripcion %>
    <%= f.text_field :descripcion %>
  </div>

  <div class="field">
    <%= f.label :imagen %>
    <%= f.text_field :imagen %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
